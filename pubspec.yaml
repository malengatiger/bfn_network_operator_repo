name: bfn_network_operator_repo
description: Network Operator App
version: 0.0.1+1
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bfnlibrary:
    path: ../bfn_mobile_library_repo

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - logo.png

